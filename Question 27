/27.Write a program to print the sum of the first n odd numbers.
#include <stdio.h>
int main()
{
    int n, i, sum = 0;

    // Input value of n
    printf("Enter the number of odd terms: ");
    scanf("%d", &n);

    // Calculate sum of first n odd numbers
    for (i = 1; i <= n; i++)
    {
        sum += (2 * i - 1);  // Formula for i-th odd number
    }

    // Display result
    printf("Sum of first %d odd numbers = %d\n", n, sum);

    return 0;
}
